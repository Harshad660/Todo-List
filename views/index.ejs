<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Modern To-Do List</title>
  <style>
    body {
      margin: 0;
      font-family: 'Poppins', sans-serif;
      display: flex;
      justify-content: center;
      align-items: center;
      min-height: 100vh;
      background: linear-gradient(135deg, hsl(243, 67%, 35%), #764ba2);
    }
    .container {
      width: 380px;
      background: rgba(255, 255, 255, 0.15);
      padding: 30px;
      border-radius: 20px;
      box-shadow: 0 8px 32px rgba(0, 0, 0, 0.3);
      backdrop-filter: blur(12px);
      color: #fff;
      animation: fadeIn 1s ease-in-out;
    }
    h1 {
      text-align: center;
      margin-bottom: 20px;
      font-size: 28px;
      font-weight: 600;
      letter-spacing: 1px;
    }
    form {
      display: flex;
      gap: 10px;
      margin-bottom: 20px;
    }
    input[type="text"] {
      flex: 1;
      padding: 12px;
      border: none;
      border-radius: 12px;
      outline: none;
      font-size: 15px;
      color: #333;
    }
    button {
      padding: 12px 18px;
      border: none;
      border-radius: 12px;
      background: linear-gradient(135deg, #ff9966, #ff5e62);
      color: white;
      font-weight: bold;
      cursor: pointer;
      transition: transform 0.2s, box-shadow 0.2s;
    }
    button:hover {
      transform: translateY(-2px);
      box-shadow: 0 4px 10px rgba(0,0,0,0.3);
    }
    ul {
      list-style: none;
      padding: 0;
      margin: 0;
    }
    li {
      background: rgba(255, 255, 255, 0.25);
      padding: 14px;
      margin-bottom: 12px;
      border-radius: 14px;
      display: flex;
      justify-content: space-between;
      align-items: center;
      transition: transform 0.2s ease, background 0.3s;
    }
    li:hover {
      transform: scale(1.02);
      background: rgba(255, 255, 255, 0.35);
    }
    .task-text {
      flex: 1;
      font-size: 16px;
      color: #fff;
    }
    .delete-btn {
      background: transparent;
      border: none;
      color: #ff4b5c;
      font-size: 18px;
      cursor: pointer;
      transition: transform 0.2s;
    }
    .delete-btn:hover {
      transform: rotate(15deg) scale(1.2);
    }
    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(-20px); }
      to { opacity: 1; transform: translateY(0); }
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>✨ To-Do List</h1>

    <!-- Add Task Form -->
    <form action="/" method="post">
      <input type="text" name="newTask" placeholder="Enter a new task..." required>
      <button type="submit">➕</button>
    </form>
    
    <!-- Task List -->
    <ul>
      <% tasks.forEach(task => { %>
        <li>
          <span class="task-text"><%= task.task %></span>
          <form action="/delete/<%= task._id %>" method="POST">
            <button type="submit" class="delete-btn">❌</button>
          </form>
        </li>
      <% }); %>
    </ul>
  </div>
</body>
</html>
